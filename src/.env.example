# Exemple de variables d'environnement pour l'application Expo
# Copiez ce fichier en `.env` ou configurez les variables d'environnement
# dans votre pipeline CI/Secrets pour les environnements de production.
# Dans le code, ces valeurs sont lues via `Constants.expoConfig?.extra` (expo-constants).

# URL du backend social (utilisée dans src/api/client.ts)
EXPO_PUBLIC_SOCIAL_API_URL=http://51.15.81.179:8000

# URL du backend vote (utilisée dans src/api/client.ts)
EXPO_PUBLIC_VOTE_API_URL=http://51.15.81.179:8001

# Exemple: pour un environnement de production, remplacez par l'URL publique:
# EXPO_PUBLIC_SOCIAL_API_URL=https://api.example.com
# EXPO_PUBLIC_VOTE_API_URL=https://vote.example.com
